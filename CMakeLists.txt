cmake_minimum_required(VERSION 3.2 FATAL_ERROR)

set (PLAYGROUND_PROJECT playground)

project (${PLAYGROUND_PROJECT})

# define some folders
get_filename_component(PARENT_DIR "${CMAKE_CURRENT_SOURCE_DIR}" PATH)
set(PLAYGROUND_DIR ${PARENT_DIR}/${PLAYGROUND_PROJECT})

MESSAGE( STATUS "PROJECT_DIR: " ${PROJECT_DIR} )

MESSAGE( STATUS "PROJECT_SOURCE_DIR: " ${PROJECT_SOURCE_DIR} )
MESSAGE( STATUS "PROJECT_BINARY_DIR: " ${PROJECT_BINARY_DIR} )

# The list of CQELS source files. Please add every new file to this list
set(PLAYGROUND_SOURCES
  ${PLAYGROUND_DIR}/src/main.cpp
)

add_executable(${PLAYGROUND_PROJECT} ${PLAYGROUND_SOURCES})

target_link_libraries(${PLAYGROUND_PROJECT} ${CMAKE_THREAD_LIBS_INIT})
